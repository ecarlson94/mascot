import 'package:flutter/rendering.dart';

const BorderRadius bigBorderRadius = BorderRadius.all(Radius.circular(28.0));
const BorderRadius mediumBorderRadius = BorderRadius.all(Radius.circular(20.0));
